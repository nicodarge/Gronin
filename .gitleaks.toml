[extend]
useDefault = true

[[rules]]
id = "puppet-eyaml"
description = "Hiera eyaml ciphertext must never reach this repository"
regex = '''ENC\[PKCS7,'''

[[rules]]
id = "claude-oauth-token"
description = "Claude Code OAuth token"
regex = '''sk-ant-(api|oat)[0-9a-zA-Z_\-]{20,}'''
