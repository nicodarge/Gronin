{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nicodarge.github.io/Gronin/playbook.schema.json",
  "title": "Gronin playbook",
  "description": "The shape of a playbook. This schema is the shape layer only: it is published so editors can complete and lint a playbook, and it is NOT the gate. The semantic refusals - path traversal resolution, MCP server existence, shell detection in a tool set, the receipt check - cannot be expressed here and live in the runtime's validator. A playbook that satisfies this schema may still be refused at load, by design. Interpolation is namespaced (${config.x} or ${trigger.x}); the shape layer does not check that either, so a bare ${x} passes here and is refused by the runtime (FR-039).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "trigger",
    "agent",
    "sinks"
  ],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{0,62}$",
      "description": "Unique across the loaded set."
    },
    "description": {
      "type": "string",
      "description": "Required by the runtime when agent.restricted is false: turning off the coarsest bound is a decision and has to be stated."
    },
    "trigger": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "enum": [
            "cron",
            "manual"
          ]
        },
        "schedule": {
          "type": "string",
          "description": "Required when type is cron. Standard five-field cron expression."
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "cron"
              }
            }
          },
          "then": {
            "required": [
              "schedule"
            ]
          }
        }
      ]
    },
    "gather": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "run",
          "as"
        ],
        "properties": {
          "run": {
            "type": "string"
          },
          "as": {
            "type": "string",
            "pattern": "^[A-Za-z0-9._-]+$"
          }
        }
      }
    },
    "agent": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "model",
        "prompt_file",
        "output_schema"
      ],
      "properties": {
        "model": {
          "type": "string"
        },
        "prompt_file": {
          "type": "string"
        },
        "restricted": {
          "type": "boolean",
          "default": true,
          "description": "Removes the command- and code-running built-in tools at the process level. Default true."
        },
        "tools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The replacing tool set. The runtime refuses a shell or a writing tool here; the schema cannot tell them apart."
        },
        "mcp": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Server names. The runtime refuses a name this deployment does not provide."
        },
        "allow": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Path-scoped file tools and individually named MCP tools. The runtime refuses an entry naming a whole server, and resolves every path before deciding."
        },
        "output_schema": {
          "description": "JSON Schema the agent report is validated against. An object, because that is what the runtime compiles: a string was accepted here and named a schema file the runtime never resolved, so it passed the load gate and failed every run after the agent had been paid for.",
          "type": "object"
        },
        "timeout": {
          "type": "string",
          "pattern": "^[0-9]+(s|m|h)$",
          "default": "30m"
        }
      }
    },
    "sinks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "minProperties": 1,
        "maxProperties": 1,
        "description": "One key naming the sink type, whose value configures it. The runtime refuses a type this deployment does not implement (FR-038) and a creating sink without a cap; neither is knowable from the shape alone."
      }
    },
    "guard": {
      "description": "Shape only: the durations of the claim set belong to the deployment and are checked by the runtime at startup, not here. An unknown key is refused (FR-119), and so is a key this runtime does not apply yet, by the load gate rather than by this schema.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "rate": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "runs",
            "per"
          ],
          "properties": {
            "runs": {
              "type": "integer",
              "minimum": 1,
              "description": "At most this many runs start within any window of `per` (FR-114). A run counts from the moment it starts, whatever its outcome."
            },
            "per": {
              "type": "string",
              "pattern": "^[1-9][0-9]*(m|h)$",
              "description": "The window, in whole minutes or hours. Keyed on the playbook name alone."
            }
          },
          "description": "A trigger this refuses is discarded, never made to wait (FR-116)."
        },
        "wait": {
          "type": "string",
          "pattern": "^[0-9]+(s|m|h)$",
          "default": "30m",
          "description": "How long a trigger refused because the playbook is already running may wait for it (FR-112). A zero duration still enters the waiting slot and expires at once, recorded as wait_expired."
        }
      }
    },
    "retrieve": {
      "description": "What the runtime searches before the agent runs, each entry written into the working directory under its `as`. Shape only, like the rest of this schema: whether a collection is declared, whether query_from names a gather step, and whether an `as` collides are the runtime's refusals. A mode, an endpoint, a model, a credential or a provider is refused as an unknown key: they belong to the deployment's collection, never to a playbook.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "collection",
          "as"
        ],
        "oneOf": [
          {
            "required": [
              "query"
            ]
          },
          {
            "required": [
              "query_from"
            ]
          }
        ],
        "properties": {
          "collection": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]{0,62}$",
            "description": "A collection the deployment's catalogue declares."
          },
          "query": {
            "type": "string",
            "minLength": 1,
            "description": "Interpolated like every other playbook string, against ${trigger.x} and ${config.x}. Searched as plain text: no word or character in it is search syntax."
          },
          "query_from": {
            "type": "string",
            "pattern": "^[A-Za-z0-9._-]+$",
            "description": "The `as` of a gather step whose output is the query."
          },
          "as": {
            "type": "string",
            "pattern": "^[A-Za-z0-9._-]+$",
            "description": "The file the results are written to in the run's working directory, where the agent reads them as it reads gathered input."
          },
          "max_results": {
            "type": "integer",
            "minimum": 1,
            "maximum": 50,
            "default": 10
          },
          "max_bytes": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65536,
            "default": 16384,
            "description": "Every byte of the results file, the lines naming each result's source included."
          }
        }
      }
    }
  }
}
