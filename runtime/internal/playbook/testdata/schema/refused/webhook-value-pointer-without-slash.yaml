name: alert-triage
trigger:
  type: webhook
  source: alerts
  values:
    alertname:
      at: alertname
      pattern: '[A-Za-z0-9_.:-]+'
      max_length: 128
agent: {model: m, prompt_file: p.md, output_schema: {type: object}}
sinks: [{discord: {channel: "#ops"}}]
