// Package config holds the deployment configuration, the state directory, and the values a playbook may interpolate against. It never reads the process environment on a playbook's behalf.
package config
