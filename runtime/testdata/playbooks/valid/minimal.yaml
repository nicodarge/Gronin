name: drift-check
trigger:
  type: cron
  schedule: "0 6 * * *"
agent:
  model: claude-sonnet-5
  prompt_file: prompt.md
  tools: [Read, Grep]
  allow: ["Read(./**)"]
  output_schema:
    type: object
    required: [findings]
sinks:
  - discord:
      webhook: ${config.ops_webhook}
