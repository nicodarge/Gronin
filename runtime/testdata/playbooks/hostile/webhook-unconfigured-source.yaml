name: webhook-unconfigured-source
trigger:
  type: webhook
  source: nope
agent:
  model: claude-sonnet-5
  prompt_file: prompt.md
  output_schema:
    type: object
sinks:
  - discord:
      webhook: ${config.ops_webhook}
