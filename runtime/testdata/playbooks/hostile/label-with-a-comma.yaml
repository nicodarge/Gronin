name: label-with-a-comma
trigger:
  type: manual
agent:
  model: claude-sonnet-5
  prompt_file: prompt.md
  tools: [Read]
  output_schema:
    type: object
sinks:
  - github:
      repo: ${config.audit_repo}
      label: doc-drift,urgent
      cap: 3
