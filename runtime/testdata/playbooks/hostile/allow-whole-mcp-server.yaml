name: allow-whole-mcp-server
trigger:
  type: manual
agent:
  model: claude-sonnet-5
  prompt_file: prompt.md
  mcp: [grafana]
  allow: [mcp__grafana]
  output_schema:
    type: object
sinks:
  - discord:
      webhook: ${config.ops_webhook}
