name: unrestricted-without-a-reason
trigger:
  type: manual
agent:
  model: claude-sonnet-5
  prompt_file: prompt.md
  restricted: false
  output_schema:
    type: object
sinks:
  - discord:
      webhook: ${config.ops_webhook}
